<%= form_with(model: sheet, local: true) do |form| %>
  <% if sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sheet.errors.count, "error") %> prohibited this sheet from being saved:</h2>

      <ul>
      <% sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :sheet_amount %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.text_field :height, id: :sheet_height %>
  </div>

  <div class="field">
    <%= form.label :width %>
    <%= form.text_field :width, id: :sheet_width %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
