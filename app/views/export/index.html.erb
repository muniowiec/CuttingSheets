<!DOCTYPE html>
<html>
<head>
  <style>
    table {
      border-collapse: collapse;
      width: 1px;
      white-space: nowrap;
    }

    th, td {
      text-align: left;
      padding: 8px;
    }

    tr:nth-child(even){background-color: #f2f2f2}

    th {
      background-color: #4CAF50;
      color: white;
    }

    .button {
      background-color: #4CAF50; /* Green */
      border: none;
      color: white;
      padding: 5px 5px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 12px;
      margin: 1px 1px;
      cursor: pointer;
      -webkit-transition-duration: 0.4s; /* Safari */
      transition-duration: 0.4s;
    }

    .button:visited {
      color: white;
    }

    .button:hover {
      box-shadow: 0 8px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
    }
  </style>
</head>
<body>

<p id="notice"><%= notice %></p>

<h1>Used algorithm: <%= @algorithm_name %></h1>

<h1>Packed boxes</h1>

<table>
  <thead>
  <tr>
    <th>Id</th>
    <th>Height</th>
    <th>Width</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% packed_boxes.each do |packed_box| %>
    <tr>
      <td><%= packed_box.id %></td>
      <td><%= packed_box.height %></td>
      <td><%= packed_box.width %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<h1>Unpacked boxes</h1>

<table>
  <thead>
  <tr>
    <th>Id</th>
    <th>Height</th>
    <th>Width</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% unpacked_boxes.each do |unpacked_box| %>
    <tr>
      <td><%= unpacked_box.id %></td>
      <td><%= unpacked_box.height %></td>
      <td><%= unpacked_box.width %></td>
    </tr>
  <% end %>
  </tbody>
</table>


<h1>Used Sheets</h1>

<table>
  <thead>
  <tr>
    <th>Id</th>
    <th>Height</th>
    <th>Width</th>
    <th>Scrap</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% used_bins.each do |usedBin| %>
    <tr>
      <td><%= usedBin.id %></td>
      <td><%= usedBin.height %></td>
      <td><%= usedBin.width %></td>
      <td><%= usedBin.is_scrap %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<h1>New Scraps From Used Sheets </h1>

<table>
  <thead>
  <tr>
    <th>Height</th>
    <th>Width</th>
    <th colspan="2"></th>
  </tr>
  </thead>

  <tbody>
  <% scraps.each do |scrap| %>
    <tr>
      <td><%= scrap.height %></td>
      <td><%= scrap.width %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'Claim Order', claim_url, :class => 'button' %>
<%= link_to 'Back', ordersheets_path, :class => 'button' %>

<br>